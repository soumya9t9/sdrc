import { SelectionFilterPipe } from './selection-filter.pipe';

describe('SelectionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
