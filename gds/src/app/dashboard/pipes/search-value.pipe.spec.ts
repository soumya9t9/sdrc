import { SearchValuePipe } from './search-value.pipe';

describe('SearchValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchValuePipe();
    expect(pipe).toBeTruthy();
  });
});
