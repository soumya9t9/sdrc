import { OptionFilterPipe } from './option-filter.pipe';

describe('OptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
