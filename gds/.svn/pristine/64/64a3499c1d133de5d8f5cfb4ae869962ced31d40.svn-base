import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InformationDailogComponent } from './information-dailog.component';

describe('InformationDailogComponent', () => {
  let component: InformationDailogComponent;
  let fixture: ComponentFixture<InformationDailogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InformationDailogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InformationDailogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
