import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DataEntryWebComponent } from './data-entry-web.component';

describe('DataEntryWebComponent', () => {
  let component: DataEntryWebComponent;
  let fixture: ComponentFixture<DataEntryWebComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DataEntryWebComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DataEntryWebComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
