import { TableNamePipe } from './table-name.pipe';

describe('TableNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TableNamePipe();
    expect(pipe).toBeTruthy();
  });
});
