import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ManageRole } from './manage-role.component';

describe('ManageRoleComponent', () => {
  let component: ManageRole;
  let fixture: ComponentFixture<ManageRole>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ManageRole ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ManageRole);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
